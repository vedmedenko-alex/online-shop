<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.solvd.online_shop.mybatisimpl.mappers.ProductMapper">

    <resultMap id="ProductResultMap" type="com.solvd.online_shop.models.Product">
        <id property="productId" column="product_id"/>
        <result property="categoryId" column="category_id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="price" column="price"/>
        <result property="stockQuantity" column="stock_quantity"/>
    </resultMap>

    <select id="getById" parameterType="int" resultMap="ProductResultMap">
        SELECT * FROM Products WHERE product_id = #{id}
    </select>

    <select id="getAll" resultMap="ProductResultMap">
        SELECT * FROM Products
    </select>

    <insert id="add" parameterType="com.solvd.online_shop.models.Product" useGeneratedKeys="true" keyProperty="productId">
        INSERT INTO Products (category_id, name, description, price, stock_quantity)
        VALUES (#{categoryId}, #{name}, #{description}, #{price}, #{stockQuantity})
    </insert>

    <update id="update" parameterType="com.solvd.online_shop.models.Product">
        UPDATE Products
        SET category_id=#{categoryId}, name=#{name}, description=#{description},
            price=#{price}, stock_quantity=#{stockQuantity}
        WHERE product_id=#{productId}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM Products WHERE product_id=#{id}
    </delete>

</mapper>

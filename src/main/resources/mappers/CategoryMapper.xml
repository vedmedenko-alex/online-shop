<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.solvd.online_shop.mybatisimpl.mappers.CategoryMapper">

    <resultMap id="CategoryResultMap" type="com.solvd.online_shop.models.Category">
        <id property="categoryId" column="category_id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
    </resultMap>

    <select id="getById" parameterType="int" resultMap="CategoryResultMap">
        SELECT * FROM Categories WHERE category_id = #{id}
    </select>

    <select id="getAll" resultMap="CategoryResultMap">
        SELECT * FROM Categories
    </select>

    <insert id="add" parameterType="com.solvd.online_shop.models.Category">
        INSERT INTO Categories (name, description) VALUES (#{name}, #{description})
    </insert>

    <update id="update" parameterType="com.solvd.online_shop.models.Category">
        UPDATE Categories SET name=#{name}, description=#{description}
        WHERE category_id=#{categoryId}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM Categories WHERE category_id=#{id}
    </delete>

</mapper>
